<style>
html, body {
  margin: 0;
  padding: 0;
  width: 100%;
  overflow-x: hidden;
}

body {
  padding-top: 80px; /* Altura do menu para outras páginas */
}

/* Específico para a página home - remove TODOS os espaçamentos */
body.home-page {
  padding-top: 0 !important;
  margin-top: 0 !important;
  padding: 0 !important;
  margin: 0 !important;
}

body.home-page * {
  margin-top: 0;
}

.menu-header {
  display: flex;
  justify-content: center;
  align-items: center;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 100;
  background-color: #160B5D;
  padding: 16px 32px;
  box-sizing: border-box;
  max-width: 100%;
  overflow-x: hidden;
  height: 80px;
  box-sizing: border-box;
}

.dstqMenu2[href="/login"] {
  color: #FFC52D;
}

.dstqMenu2[href="/cadastro"] {
  color: #FFC52D;
}

.menu-icon {
  font-size: 1.5rem;
  background: none;
  border: none;
  color: white;
  cursor: pointer;
  position: absolute;
  left: 32px;
  top: 50%;
  transform: translateY(-50%);
}

.logo img {
  height: 48px;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;  
}

.icons {
  display: flex;
  gap: 24px;
  position: absolute;
  right: 32px;
  top: 50%;
  transform: translateY(-50%);
  align-items: center;
}

.login-btn {
  font-family: 'Poppins', sans-serif;
  font-weight: 600;
  font-size: 14px;
  transition: all 0.3s ease;
}

.login-btn:hover {
  color: #ffffff !important;
}

.icone {
  font-size: 1.25rem;
  background: none;
  border: none;
  color: white;
  cursor: pointer;
}

.mobile-menu {
  background-color: #160B5D;
  display: block;
  position: fixed;
  top: 0;
  left: 0;
  height: 100vh;
  width: min(400px, 100%);
  transform: translateX(-100%);
  transition: transform 0.4s cubic-bezier(.77,0,.18,1);
  z-index: 150;
  padding: 32px 24px;
  overflow-y: auto;
  box-shadow: 8px 0 32px rgba(0,0,0,0.2);
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
}

.mobile-menu.open {
  transform: translateX(0);
}

#menuOverlay {
  position: fixed;
  top: 0; left: 0;
  width: 100vw; height: 100vh;
  background: rgba(0,0,0,0.5);
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.4s;
  z-index: 99;
}

#menuOverlay.open {
  opacity: 1;
  pointer-events: all;
}

.mobile-menu ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.mobile-menu li {
  margin: 24px 0;
  border-bottom: 1px solid #fff;
  padding-bottom: 16px;
}

.mobile-menu a {
  color: white;
  text-decoration: none;
  font-size: 1.1rem;
}

.mobile-menu a:hover {
  color: #00C4FF;
}

.visitante {
  font-weight: bold;
}
.mobile-menu p {
  color: white;
  font-family: 'Poppins', sans-serif;
  font-size: 1rem;
}

.close-mobile-menu {
  position: fixed;
  top: 16px;
  right: max(16px, calc(50% - 200px));
  background: none;
  border: none;
  font-size: 2rem;
  color: #fff;
  cursor: pointer;
  z-index: 200;
  padding: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
}

  .close-mobile-menu {
    right: 8px;
  }

.dstqMenu2 {
  color: #FFC52D;
  font-weight: inherit;
  text-decoration: none;
}
.conector {
  color: white;
  font-weight: 300;
}

.menu-footer {
  position: absolute;
  bottom: 20px;
  left: 0;
  right: 0;
  padding: 0 30px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.menu-footer a {
  color: white;
  text-decoration: none;
  font-size: 14px;
  font-weight: 400;
  transition: color 0.2s ease;
}

.menu-footer a:hover {
  color: white;
}

@media (max-width: 640px) {
  .menu-header {
    padding: 16px;
  }
  
  .menu-icon {
    left: 16px;
  }
  
  .icons {
    right: 16px;
  }
  
  .logo img {
    height: 40px;
  }

  /* Removido padding-top: 0px para manter o menu funcionando */
}


</style>
<header class="menu-header">
  <button class="menu-icon" id="menuHamburguer" aria-label="Abrir menu">
    ☰
  </button>
  <span class="logo">
    <img src="/img/bomba.svg" alt="Logo Ápice">
  </span>
  <span class="icons">
    <a href="/perfil"><button class="icone" aria-label="Perfil"><img src="/img/Icon.png"></button></a>
  </span>
  <nav class="mobile-menu" id="mobileMenu">
  <button class="close-mobile-menu" aria-label="Fechar menu">&times;</button>
    <ul>
      <p>Olá, <span class="visitante">visitante</span><br>
      <a href="/login" class="dstqMenu2">Entre</a> <span class="conector">ou</span> <a href="/cadastro" class="dstqMenu2">Cadastre-se</a></p>
      <li><a href="/">Home</a></li>
      <li><a href="/vagas">Vagas</a></li>
      <li><a href="/empresa">Empresas</a></li>
    </ul>
    <div class="menu-footer">
      <a href="/fale-conosco">Fale Conosco</a>
      <a href="/sobre-nos">Sobre nós</a>
    </div>
  </nav>
  <span id="menuOverlay"></span>
</header>
<script src="/js/script.js" defer></script>